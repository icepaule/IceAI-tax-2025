# Installation

1. `cd /root/tax-ai-stack`
2. `make bootstrap`
3. `cp -n .env.example .env`
4. `.env` befuellen
5. `make up`
6. Optional Status-Web: `make status`

## Voraussetzungen
- Docker + Docker Compose
- Netzwerkzugriff auf Ollama, ecoDMS, Mailserver
